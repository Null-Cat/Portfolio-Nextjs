# Public Cloudflare Turnstile site key. This is NOT a secret: it is embedded in
# the client bundle and visible in the browser. It is committed so the production
# build (Cloudflare Workers Builds) can inline NEXT_PUBLIC_* values at build time.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAAB6zJH1fLSSBEoe9
